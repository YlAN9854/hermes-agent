# CLAUDE.md — ContextVis

> ContextVis 的**设计理念蒸馏**。本文件只定义**与渲染形式无关**的不变量——
> 无论最终是 web 组件、终端 TUI（xterm），还是别的形态，这些都成立。

## 使命

Agent 的上下文窗口是它的工作记忆，却是个黑盒。
ContextVis 的唯一使命：**把它变成可读、可追溯、可治理的白盒**。一切设计服务于此。

## 设计理念

1. **占用对抗上限，是中心真相。** 上下文的本质是"在固定容量里不断填充"。任何形态都必须先答：装了多少？还剩多少？离上限多远？
2. **一个隐喻贯穿全局。** 用单一空间隐喻统一表达结构、演变、压缩、干预，降低认知负荷。不要每个功能换一套视觉语言。
3. **量级编码 token。** token 是唯一核心度量，必须映射成某种"量级"——web 里是面积，终端里是字符行程/块数。比例关系神圣不可破。
4. **类型恒定可辨。** 内容分一组固定小类型，在所有形态中以恒定方式区分（web 用色，终端用 ANSI 色/字形）。
5. **时间是一条轴。** 上下文随轮次累积：后一轮 = 前一轮全部 + 本轮注入。让"怎么涨到现在"可回放。
6. **干预必须透明。** 永不静默改动上下文。每次删除/折叠/保留都要让用户看到"改了什么 + 系统如何修复衔接"。这是信任的根基。
7. **白盒优先于美观。** 克制、高密度、忠于数据；宁可朴素，不要装饰性噪音。

## 概念模型（形态无关）

上下文 = 一组**块（chunk）**，填进一个有**预算上限**的容器。

- **块**：类型 · token 量级 · 所属轮次 · 可选原文 · 可选命运。
- **容器**：预算（模型窗口大小）· 当前占用 · 可能的压缩事件。
- **压缩** = 给历史块分配命运：保留 / 折叠为摘要 / 删除。
- **联动** = 一切围绕"当前聚焦轮次 + 选中块"协同。

数据契约（这是**模型**，与渲染无关）：

```
ChunkType = system | user | assistant | tool_call | tool_result | file
ContextChunk   { id, type, tokens, turn, label, raw?, fate? }
ContextSnapshot{ chunks[], budget, turn, compaction? }
```

- `tokens` 必须是真实计数；`budget` 必须是真实窗口。整套可信度系于此。
- 类型按宿主真实概念映射；`file` = 读文件类 / 超大体量的 tool_result（膨胀主因，值得单列）。

## 渲染可插拔

同一个 snapshot 可渲染成不同形态。**理念不变，皮肤可换**：

| 概念 | Web 组件 | 终端 TUI（xterm） |
|---|---|---|
| 容器 + 占用 | 储液瓶，液面高度 | 块字符槽 `█`，填充长度 |
| 块的 token 量级 | 面积（树图） | 连续色块的行程长度 |
| 类型 | 分类色 | ANSI/256 色 或 字形 |
| 逐轮演变 | 多瓶级联 + 水滴 | 占用率 sparkline / 逐帧增长 |
| 压缩 | 液面骤降 | 槽位回落 |

> 选形态时只问一句：它能不能讲清**占用 / 构成 / 累积 / 命运**？能，就是合格的 ContextVis。

## 集成姿态（三层隔离）

1. **适配器**（宿主专属）：真实上下文 → snapshot。
2. **核心**（形态无关的模型 + 一种渲染）：只吃 snapshot，绝不直接读宿主状态。
3. **挂载壳**（宿主专属）：位置 + 与现有对话界面的联动。

> 黄金法则：渲染层永不直接依赖宿主数据结构；耦合全部收敛到适配器与挂载壳。

## 取舍

宿主（Hermes dashboard）已有终端对话。**保留 ContextVis 的独有价值——容器/占用视图；复用宿主已有的对话与原文展示**，靠选中联动而非重建。

## 开放问题（动手前确认）

- 最终形态：嵌入 TUI / 独立 web 组件 / 浮层？
- 每条消息是否带真实 token 数？无则需估算。
- 是否有 compaction / 历史快照？决定"演变/压缩"能否落地。
- 上下文是否可编辑并重新拼接？决定干预是真功能还是只读建议。
